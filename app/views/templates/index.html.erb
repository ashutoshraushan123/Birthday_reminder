
<h4><em>LIST OF TEMPLATES<em></h4>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
<%#= link_to 'Back', people_path %>

<% @templates.each do |template| %>

  <tr>
    
    <th><h4><input type="radio" id="<%= template.id %>" name="group1" value="<%= template.id %>"></h4></th>

    <td><%= template.html.html_safe %></td>
    <td><%= link_to 'Show', template %></td> 
    <td><%= link_to 'Edit', edit_template_path(template) %></td>
    <td><%#= link_to 'Destroy', template, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>



<br />

<%= link_to 'Back', people_path %>
